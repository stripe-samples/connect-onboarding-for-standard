# frozen_string_literal: true

# NOTE: This file manages dependencies used for automated testing of the
# sample, feel free to ignore this and the /spec directory.

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'rspec'
gem 'rest-client'
gem 'byebug'
gem 'stripe'
gem 'dotenv'

gem 'selenium-webdriver'
gem 'capybara'
gem 'capybara-screenshot'

gem 'rspec-github', require: false
